# Phase 3 — summary implementation
def generate_daily_summary(for_date=None) -> dict:
    return {
        "success": False,
        "message": "⏳ Daily summary is coming in **Phase 3** (uses local Hermes-3).",
    }


def generate_weekly_summary(week=None) -> dict:
    return {
        "success": False,
        "message": "⏳ Weekly summary is coming in **Phase 3** (uses Groq API free tier).",
    }
