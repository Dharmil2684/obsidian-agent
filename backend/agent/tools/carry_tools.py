# Phase 3 — carry-forward implementation
def carry_tasks_forward(from_date=None, to_date=None) -> dict:
    return {
        "success": False,
        "message": (
            "⏳ Carry-forward is coming in **Phase 3**.\n\n"
            "For now, check `/status` and manually note what to do tomorrow."
        ),
    }
